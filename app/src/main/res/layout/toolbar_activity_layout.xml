<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundDefault"
    >

    <LinearLayout
        android:id="@+id/layoutToolbarActivityStatusBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?themeColorPrimaryDark"
        android:elevation="@dimen/toolbarElevation"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        />

    <com.xephorium.crystalnote.ui.custom.NoteToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?themeColorPrimary"
        android:elevation="@dimen/toolbarElevation"
        app:contentInsetStart="0dp"
        app:layout_constraintTop_toBottomOf="@id/layoutToolbarActivityStatusBar"
        />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/layoutActivityContent"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        />

</androidx.constraintlayout.widget.ConstraintLayout>